<Page x:Class="SistemaPedidos.Views.Pages.PedidoList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SistemaPedidos.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PedidoList"
      Background="White" Loaded="Page_Loaded">
    
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🔹 TÍTULO -->
        <TextBlock Grid.Row="0" Text="Cadastro de Pedidos"
           FontSize="22"
           FontWeight="Bold"
           Margin="0,0,0,10"/>

        <Grid Grid.Row="1" HorizontalAlignment="Left">
            <Button Margin="2" Style="{StaticResource SuccessButton}" Click="btnCadastrar_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/img/plus.png" Width="16" Height="16" Margin="0,0,5,0"/>
                    <TextBlock Text="Cadastrar"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- 🔹 FILTROS -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Pessoa -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Pessoa"/>
                <ComboBox x:Name="cbPessoas" Height="30" SelectedIndex="-1" Text="Selecione..."/>
            </StackPanel>

            <!-- Status -->
            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="Status"/>
                <ComboBox x:Name="cbStatus" Height="30" SelectedIndex="-1" Width="120" Text="Selecione..."/>
            </StackPanel>

            <!-- Pagamento -->
            <StackPanel Grid.Column="2" Margin="0,0,10,0">
                <TextBlock Text="Pagamento"/>
                <ComboBox x:Name="cbPagamento" Height="30" SelectedIndex="-1" Width="70" Text="Selecione..."/>
            </StackPanel>

            <!-- Periodo -->
            <StackPanel Grid.Column="3" Margin="0,0,10,0">
                <TextBlock Text="Período"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DatePicker x:Name="txtDataInicial" Grid.Column="0" Width="120"/>
                    <DatePicker x:Name="txtDataFinal" Grid.Column="2" Width="120"/>
                </Grid>
            </StackPanel>

            <!-- Botão Pesquisar -->
            <Button Grid.Column="4" Margin="0,10,0,0" Style="{StaticResource PrimaryButton}" Click="btnPesquisar_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/img/search.png" Width="16" Height="16" Margin="0,0,5,0"/>
                    <TextBlock Text="Pesquisar"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- 🔹 DATA GRID -->
        <DataGrid Grid.Row="3"
          ItemsSource="{Binding Pedido}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          x:Name="dataGridPedidos">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Pessoa" Binding="{Binding Pessoa.Nome}" Width="2*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="70"/>
                <DataGridTextColumn Header="Pagamento" Binding="{Binding Pagamento}" Width="75"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataVenda, StringFormat=dd/MM/yyyy}" Width="75"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding ValorTotal, StringFormat=F2}" Width="100"/>

                <!-- Ações -->
                <DataGridTemplateColumn Header="Ações" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">

                                <Button Margin="2" Style="{StaticResource SuccessButton}" Click="btnVisualizar_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="/Resources/img/eye.png" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock Text="Visualizar"/>
                                    </StackPanel>
                                </Button>

                                <!-- Status -->
                                <Button Margin="2" Style="{StaticResource PrimaryButton}" Click="btnStatus_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="/Resources/img/pencil.png" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock Text="Status"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

        <!-- 🔹 TOTAL REGISTROS -->
        <TextBlock Grid.Row="4"
           FontWeight="SemiBold"
           Margin="0,10,0,0"
           x:Name="lblQuantidade"/>
    </Grid>
</Page>
