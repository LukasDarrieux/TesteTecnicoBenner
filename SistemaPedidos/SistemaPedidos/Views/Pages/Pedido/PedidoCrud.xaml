<Page x:Class="SistemaPedidos.Views.Pages.PedidoCrud"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SistemaPedidos.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PedidoCrud"
      Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Cadastro de Pedido"
           FontSize="20"
           FontWeight="Bold"
           Margin="0,0,0,15"
           x:Name="lblTitulo"/>

        <!-- Conteúdo -->
        <StackPanel Grid.Row="1">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <Label Content="Pessoa"/>
                    <ComboBox x:Name="cbPessoa" Height="30" SelectedIndex="-1" Text="Selecione..."/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Label Content="Pagamento"/>
                    <ComboBox x:Name="cbPagamento" Height="30" SelectedIndex="-1" Text="Selecione..."/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10,0,0,0">
                    <Label Content="Data"/>
                    <TextBox x:Name="txtData" Height="30" IsReadOnly="True"/>
                </StackPanel>
            </Grid>

        </StackPanel>

        <StackPanel Grid.Row="2" Margin="10,0,0,10" HorizontalAlignment="Right">
            <Button x:Name="btnAdicionar" Margin="2" Style="{StaticResource SuccessButton}" Width="120" Click="btnAdicionar_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/img/plus.png" Width="16" Height="16" Margin="0,0,5,0"/>
                    <TextBlock Text="Adicionar Item"/>
                </StackPanel>
            </Button>
        </StackPanel>


        <GroupBox Grid.Row="3" Header="Itens do Pedido" Margin="0,0,0,0">
            <DataGrid AutoGenerateColumns="False" CanUserAddRows="True" IsReadOnly="True" x:Name="dataGridItens">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Produto" Binding="{Binding Produto.Nome}" Width="2*"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="Auto"/>
                    <DataGridTextColumn Header="Preço Unitário" Binding="{Binding Produto.Valor}" Width="Auto"/>
                    <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal}" Width="Auto"/>

                    <!-- Ações -->
                    <DataGridTemplateColumn Header="Ações" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <StackPanel Orientation="Horizontal">

                                    <Button x:Name="btnRemover" Margin="2" Style="{StaticResource DangerButton}" Click="btnRemover_Click" Width="90">
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="/Resources/img/trash.png" Width="16" Height="16" Margin="0,0,5,0"/>
                                            <TextBlock Text="Remover"/>
                                        </StackPanel>
                                    </Button>

                                </StackPanel>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>
        </GroupBox>

           
        <Label Grid.Row="4" Content="Quantidade: " FontSize="12" FontWeight="SemiBold" x:Name="lblQuantidade"/>
        <Label Grid.Row="5" Content="Valor Total: " FontSize="12" FontWeight="SemiBold" x:Name="lblValorTotal"/>
        
        <!-- Botões -->
        <StackPanel Grid.Row="6"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,20,0,0">

            <Button x:Name="btnCancelar"
            Content="Cancelar"
            Style="{StaticResource SecondaryButton}"
            Margin="0,0,10,0"
            Click="btnCancelar_Click"/>

            <Button 
            x:Name="btnSalvar"
            Content="Salvar"
            Style="{StaticResource PrimaryButton}"
            Click="btnSalvar_Click"/>
        </StackPanel>
    </Grid>
</Page>
